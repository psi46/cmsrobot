<?xml version="1.0" encoding="utf-8"?>
<Programs xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.staubli.com/robotics/VAL3/Program/2">
  <Program name="moveDown">
    <Parameters xmlns="http://www.staubli.com/robotics/VAL3/Param/1">
      <Parameter name="where" type="pointRs" xsi:type="element" />
      <Parameter name="finalSpeed" type="mdesc" xsi:type="element" />
      <Parameter name="finalAppro" type="trsf" xsi:type="element" />
      <Parameter name="meticulous" type="bool" xsi:type="element" />
    </Parameters>
    <Locals>
      <Local name="speed" type="mdesc" xsi:type="array" size="3" />
    </Locals>
    <Code><![CDATA[begin
  // Note: finalSpeed is not taken literally!
  if finalSpeed == mVerySlow
    speed[0] = mFast
    speed[1] = mSlow
    speed[2] = mVerySlow
  elseIf finalSpeed == mSlow
    speed[0] = mFast
    speed[1] = mNomSpeed
    speed[2] = mVerySlow
  else 
    speed[0] = mFast
    speed[1] = mFast
    speed[2] = mSlow
  endIf
  
  //  if meticulous
  //    movej(appro(where, trZverySafe), tTool, speed[0])
  //  endIf
  
  movej(appro(where, trZsafe*trXY), tTool, speed[0])
  if meticulous
    waitEndMove()
  endIf
  // -- move down with decreasing speeds. Not sure whether this gets "blended" away
  movej(appro(where, trZclose*trXY), tTool, speed[1])
  movej(appro(where, finalAppro*trXY), tTool, speed[2])
  waitEndMove()
end]]></Code>
  </Program>
</Programs>